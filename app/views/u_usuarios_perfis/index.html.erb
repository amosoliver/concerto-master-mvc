<div class="page-shell">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(UUsuarioPerfil) %></h1>
    </div>
    <div class="page-header__actions">
      <%= link_to "Novo #{UUsuarioPerfil.model_name.human}", new_u_usuario_perfil_path, class: "primary-button" %>
    </div>
  </div>

  <%= search_form_for @q, url: u_usuarios_perfis_path, method: :get, class: "search-panel" do |f| %>
    <div class="search-input">
      <span aria-hidden="true">🔎</span>
      <%= f.select :u_perfil_id_eq, options_from_collection_for_select(UPerfil.all, :id, :to_s, @q&.send(:u_perfil_id_eq)), { include_blank: true }, "aria-label": UUsuarioPerfil.human_attribute_name(:u_perfil_id), class: "search-input__field" %>
    </div>
    <div class="search-input">
      <span aria-hidden="true">🔎</span>
      <%= f.select :g_usuario_id_eq, options_from_collection_for_select(GUsuario.all, :id, :to_s, @q&.send(:g_usuario_id_eq)), { include_blank: true }, "aria-label": UUsuarioPerfil.human_attribute_name(:g_usuario_id), class: "search-input__field" %>
    </div>
    <div class="search-panel__actions">
      <%= f.submit "Filtrar", class: "primary-button" %>
      <%= link_to "Limpar", u_usuarios_perfis_path, class: "secondary-button" %>
    </div>
  <% end %>

  <section class="panel-card" data-controller="view-toggle">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Listagem</p>
        <h3><%= pluralize(@u_usuarios_perfis.size, "registro") %></h3>
      </div>
      <div class="view-switch">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable">☰ Tabela</button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards">▦ Cards</button>
      </div>
    </div>

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= UUsuarioPerfil.human_attribute_name(:u_perfil_id) %></th>
            <th scope="col"><%= UUsuarioPerfil.human_attribute_name(:g_usuario_id) %></th>
            <th scope="col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @u_usuarios_perfis.each do |u_usuario_perfil| %>
            <tr>
              <td><%= u_usuario_perfil.u_perfil %></td>
              <td><%= u_usuario_perfil.g_usuario %></td>
              <td>
                <%= render "shared/actions_menu", show_path: u_usuario_perfil_path(u_usuario_perfil), edit_path: edit_u_usuario_perfil_path(u_usuario_perfil), delete_path: u_usuario_perfil_path(u_usuario_perfil) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @u_usuarios_perfis.each do |u_usuario_perfil| %>
        <article class="record-card">
          <div class="record-card__body">
            <span class="detail-label"><%= UUsuarioPerfil.human_attribute_name(:u_perfil_id) %></span>
            <span class="detail-value"><%= u_usuario_perfil.u_perfil %></span>
            <span class="detail-label"><%= UUsuarioPerfil.human_attribute_name(:g_usuario_id) %></span>
            <span class="detail-value"><%= u_usuario_perfil.g_usuario %></span>
          </div>
          <div class="record-card__footer">
            <%= render "shared/actions_menu", show_path: u_usuario_perfil_path(u_usuario_perfil), edit_path: edit_u_usuario_perfil_path(u_usuario_perfil), delete_path: u_usuario_perfil_path(u_usuario_perfil) %>
          </div>
        </article>
      <% end %>
    </div>

    <% if @pagy %>
      <div class="pagination-wrap">
        <%== @pagy.series_nav %>
      </div>
    <% end %>
  </section>
</div>
