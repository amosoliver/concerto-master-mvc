<aside class="sidebar" data-sidebar-target="panel">
  <div class="sidebar__brand">
    <div class="brand-mark">🎵</div>
    <div>
      <h1>Concreto Master</h1>
      <p>Sistema de Música</p>
    </div>
    <button type="button" class="sidebar-close" data-action="click->sidebar#close" aria-label="Fechar menu">✕</button>
  </div>

  <nav class="sidebar__nav" aria-label="Menu principal">
    <% sidebar_nav_items.each do |item| %>
      <a href="<%= item[:path] || '#' %>" class="nav-item<%= ' nav-item--active' if item[:controller] && item[:controller] == controller_name %>">
        <span class="nav-item__icon"><%= item[:icon] %></span>
        <span><%= item[:label] %></span>
        <% if item[:badge] %><span class="nav-item__badge"><%= item[:badge] %></span><% end %>
      </a>
    <% end %>
  </nav>
</aside>
