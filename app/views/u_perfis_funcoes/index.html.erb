<div class="page-shell" data-controller="filters" data-filters-active-value="<%= request.query_parameters.except("page").present? %>">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(UPerfilFuncao) %></h1>
    </div>
    <div class="page-header__actions">
      <button type="button" class="filter-toggle" data-filters-target="toggleButton" data-action="click->filters#toggle" aria-expanded="false">
        <span aria-hidden="true">🔎</span> Filtros
      </button>
      <%= authorized_link_to "Novo #{UPerfilFuncao.model_name.human}", new_u_perfil_funcao_path, class: "primary-button" %>
    </div>
  </div>

  <div class="search-panel" data-filters-target="panel" hidden>
    <%= search_form_for @q, url: u_perfis_funcoes_path, method: :get do |f| %>
      <div class="search-grid">
        <div class="search-field">
          <label><%= UPerfilFuncao.human_attribute_name(:u_perfil_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :u_perfil_id_eq, options_from_collection_for_select(UPerfil.all, :id, :to_s, @q&.send(:u_perfil_id_eq)), { include_blank: true }, "aria-label": UPerfilFuncao.human_attribute_name(:u_perfil_id), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= UPerfilFuncao.human_attribute_name(:u_funcao_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :u_funcao_id_eq, options_from_collection_for_select(UFuncao.all, :id, :to_s, @q&.send(:u_funcao_id_eq)), { include_blank: true }, "aria-label": UPerfilFuncao.human_attribute_name(:u_funcao_id), class: "search-input__field" %>
          </div>
        </div>
      </div>
      <div class="search-panel__actions">
        <%= f.submit "Filtrar", class: "primary-button" %>
        <%= link_to "Limpar", u_perfis_funcoes_path, class: "secondary-button" %>
      </div>
    <% end %>
  </div>

  <section class="panel-card" data-controller="view-toggle">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Listagem</p>
        <h3><%= pluralize(@u_perfis_funcoes.size, "registro") %></h3>
      </div>
      <div class="view-switch">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable">☰ Tabela</button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards">▦ Cards</button>
      </div>
    </div>

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= UPerfilFuncao.human_attribute_name(:u_perfil_id) %></th>
            <th scope="col"><%= UPerfilFuncao.human_attribute_name(:u_funcao_id) %></th>
            <th scope="col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @u_perfis_funcoes.each do |u_perfil_funcao| %>
            <tr>
              <td><%= u_perfil_funcao.u_perfil %></td>
              <td><%= u_perfil_funcao.u_funcao %></td>
              <td>
                <%= render "shared/actions_menu", show_path: u_perfil_funcao_path(u_perfil_funcao), edit_path: edit_u_perfil_funcao_path(u_perfil_funcao), delete_path: u_perfil_funcao_path(u_perfil_funcao) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @u_perfis_funcoes.each do |u_perfil_funcao| %>
        <article class="record-card">
          <div class="record-card__body">
            <span class="detail-label"><%= UPerfilFuncao.human_attribute_name(:u_perfil_id) %></span>
            <span class="detail-value"><%= u_perfil_funcao.u_perfil %></span>
            <span class="detail-label"><%= UPerfilFuncao.human_attribute_name(:u_funcao_id) %></span>
            <span class="detail-value"><%= u_perfil_funcao.u_funcao %></span>
          </div>
          <div class="record-card__footer">
            <%= render "shared/actions_menu", show_path: u_perfil_funcao_path(u_perfil_funcao), edit_path: edit_u_perfil_funcao_path(u_perfil_funcao), delete_path: u_perfil_funcao_path(u_perfil_funcao) %>
          </div>
        </article>
      <% end %>
    </div>

    <%= render "layouts/pagination", pagy: @pagy %>
  </section>
</div>
