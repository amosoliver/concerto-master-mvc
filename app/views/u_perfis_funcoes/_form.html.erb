<%= form_with(model: u_perfil_funcao, local: true, class: "panel-card form-card", data: { controller: "select" }) do |form| %>
  <%= render "shared/form_errors", record: u_perfil_funcao %>

  <div class="form-grid">
    <div class="field-group">
      <%= form.label :u_perfil_id %>
      <%= form.collection_select :u_perfil_id, UPerfil.all, :id, :to_s, { include_blank: "Selecione um perfil..." }, class: "form-control" %>
    </div>
    <div class="field-group">
      <%= form.label :u_funcao_id %>
      <%= form.collection_select :u_funcao_id, UFuncao.all, :id, :to_s, { include_blank: "Selecione uma função..." }, class: "form-control" %>
    </div>
  </div>

  <div class="form-actions">
    <%= form.submit class: "primary-button" %>
    <%= link_to "Cancelar", u_perfil_funcao_path(@u_perfil_funcao), class: "secondary-button" if u_perfil_funcao.persisted? %>
    <%= link_to "Cancelar", u_perfis_funcoes_path, class: "secondary-button" unless u_perfil_funcao.persisted? %>
  </div>
<% end %>
