<div class="page-shell" data-controller="filters view-toggle" data-filters-active-value="<%= request.query_parameters.except("page").present? %>">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(GEntidade) %></h1>
    </div>
    <div class="page-header__actions">
      <button type="button" class="filter-toggle" data-filters-target="toggleButton" data-action="click->filters#toggle" aria-expanded="false">
        <%= ui_icon(:search, size: 16, classes: "search-input__icon") %><span>Filtros</span>
      </button>
      <div class="view-switch view-switch--header">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable"><%= ui_icon(:table, size: 15) %><span>Tabela</span></button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards"><%= ui_icon(:layout_grid, size: 15) %><span>Cards</span></button>
      </div>
      <%= authorized_link_to "Novo #{GEntidade.model_name.human}", new_g_entidade_path, class: "primary-button" %>
    </div>
  </div>

  <div class="search-panel" data-filters-target="panel" hidden>
    <%= search_form_for @q, url: g_entidades_path, method: :get do |f| %>
      <div class="search-grid">
        <div class="search-field">
          <label><%= GEntidade.human_attribute_name(:descricao) %></label>
          <div class="search-input">
            <%= ui_icon(:search, size: 16, classes: "search-input__icon") %>
            <%= f.search_field :descricao_cont, placeholder: GEntidade.human_attribute_name(:descricao), "aria-label": GEntidade.human_attribute_name(:descricao), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= GEntidade.human_attribute_name(:g_estado_id) %></label>
          <div class="search-input">
            <%= ui_icon(:search, size: 16, classes: "search-input__icon") %>
            <%= f.select :g_estado_id_eq, options_from_collection_for_select(GEstado.all, :id, :to_s, @q&.send(:g_estado_id_eq)), { include_blank: true }, "aria-label": GEntidade.human_attribute_name(:g_estado_id), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= GEntidade.human_attribute_name(:g_municipio_id) %></label>
          <div class="search-input">
            <%= ui_icon(:search, size: 16, classes: "search-input__icon") %>
            <%= f.select :g_municipio_id_eq, options_from_collection_for_select(GMunicipio.all, :id, :to_s, @q&.send(:g_municipio_id_eq)), { include_blank: true }, "aria-label": GEntidade.human_attribute_name(:g_municipio_id), class: "search-input__field" %>
          </div>
        </div>
      </div>
      <div class="search-panel__actions">
        <%= f.submit "Filtrar", class: "primary-button" %>
        <%= link_to "Limpar", g_entidades_path, class: "secondary-button" %>
      </div>
    <% end %>
  </div>

  <section class="panel-card panel-card--fill">

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= GEntidade.human_attribute_name(:descricao) %></th>
            <th scope="col"><%= GEntidade.human_attribute_name(:g_estado_id) %></th>
            <th scope="col"><%= GEntidade.human_attribute_name(:g_municipio_id) %></th>
            <th scope="col"><%= GEntidade.human_attribute_name(:g_entidade_id) %></th>
            <th scope="col" class="table-actions-col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @g_entidades.each do |g_entidade| %>
            <tr>
              <td><%= g_entidade.descricao %></td>
              <td><%= g_entidade.g_estado %></td>
              <td><%= g_entidade.g_municipio %></td>
              <td><%= g_entidade.g_entidade %></td>
              <td class="table-actions-cell">
                <%= render "shared/actions_menu", show_path: g_entidade_path(g_entidade), edit_path: edit_g_entidade_path(g_entidade), delete_path: g_entidade_path(g_entidade) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @g_entidades.each do |g_entidade| %>
        <article class="record-card">
          <div class="record-card__body">
            <div class="record-card__header">
              <h3 class="record-card__title"><%= g_entidade.descricao %></h3>
            </div>
            <div class="record-card__attrs">
              <div class="record-card__attr">
                <span class="record-card__attr-icon"><%= ui_icon(:world, size: 16) %></span>
                <span class="record-card__attr-text">
                  <span class="detail-label"><%= GEntidade.human_attribute_name(:g_estado_id) %></span>
                  <span class="detail-value"><%= g_entidade.g_estado %></span>
                </span>
              </div>
              <div class="record-card__attr">
                <span class="record-card__attr-icon"><%= ui_icon(:map_pin, size: 16) %></span>
                <span class="record-card__attr-text">
                  <span class="detail-label"><%= GEntidade.human_attribute_name(:g_municipio_id) %></span>
                  <span class="detail-value"><%= g_entidade.g_municipio %></span>
                </span>
              </div>
              <div class="record-card__attr">
                <span class="record-card__attr-icon"><%= ui_icon(:hierarchy, size: 16) %></span>
                <span class="record-card__attr-text">
                  <span class="detail-label"><%= GEntidade.human_attribute_name(:g_entidade_id) %></span>
                  <span class="detail-value"><%= g_entidade.g_entidade %></span>
                </span>
              </div>
            </div>
          </div>
          <%= render "shared/card_actions", show_path: g_entidade_path(g_entidade), edit_path: edit_g_entidade_path(g_entidade), delete_path: g_entidade_path(g_entidade) %>
        </article>
      <% end %>
    </div>

    <%= render "layouts/pagination", pagy: @pagy %>
  </section>
</div>
