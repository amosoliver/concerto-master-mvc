<div class="page-shell" data-controller="filters" data-filters-active-value="<%= request.query_parameters.except("page").present? %>">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(GPessoasInstrumento) %></h1>
    </div>
    <div class="page-header__actions">
      <button type="button" class="filter-toggle" data-filters-target="toggleButton" data-action="click->filters#toggle" aria-expanded="false">
        <span aria-hidden="true">🔎</span> Filtros
      </button>
      <%= authorized_link_to "Novo #{GPessoasInstrumento.model_name.human}", new_g_pessoas_instrumento_path, class: "primary-button" %>
    </div>
  </div>

  <div class="search-panel" data-filters-target="panel" hidden>
    <%= search_form_for @q, url: g_pessoas_instrumentos_path, method: :get do |f| %>
      <div class="search-grid">
        <div class="search-field">
          <label><%= GPessoasInstrumento.human_attribute_name(:g_pessoa_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :g_pessoa_id_eq, options_from_collection_for_select(tenant_pessoa_scope, :id, :to_s, @q&.send(:g_pessoa_id_eq)), { include_blank: true }, "aria-label": GPessoasInstrumento.human_attribute_name(:g_pessoa_id), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= GPessoasInstrumento.human_attribute_name(:g_instrumento_naipe_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :g_instrumento_naipe_id_eq, options_from_collection_for_select(tenant_instrumento_scope.sort_by(&:to_s), :id, :to_s, @q&.send(:g_instrumento_naipe_id_eq)), { include_blank: true }, "aria-label": GPessoasInstrumento.human_attribute_name(:g_instrumento_naipe_id), class: "search-input__field" %>
          </div>
        </div>
      </div>
      <div class="search-panel__actions">
        <%= f.submit "Filtrar", class: "primary-button" %>
        <%= link_to "Limpar", g_pessoas_instrumentos_path, class: "secondary-button" %>
      </div>
    <% end %>
  </div>

  <section class="panel-card" data-controller="view-toggle">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Listagem</p>
        <h3><%= pluralize(@g_pessoas_instrumentos.size, "registro") %></h3>
      </div>
      <div class="view-switch">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable">☰ Tabela</button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards">▦ Cards</button>
      </div>
    </div>

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= GPessoasInstrumento.human_attribute_name(:g_pessoa_id) %></th>
            <th scope="col"><%= GPessoasInstrumento.human_attribute_name(:g_instrumento_naipe_id) %></th>
            <th scope="col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @g_pessoas_instrumentos.each do |g_pessoas_instrumento| %>
            <tr>
              <td><%= g_pessoas_instrumento.g_pessoa %></td>
              <td><%= g_pessoas_instrumento.g_instrumento_naipe %></td>
              <td>
                <%= render "shared/actions_menu", show_path: g_pessoas_instrumento_path(g_pessoas_instrumento), edit_path: edit_g_pessoas_instrumento_path(g_pessoas_instrumento), delete_path: g_pessoas_instrumento_path(g_pessoas_instrumento) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @g_pessoas_instrumentos.each do |g_pessoas_instrumento| %>
        <article class="record-card">
          <div class="record-card__body">
            <span class="detail-label"><%= GPessoasInstrumento.human_attribute_name(:g_pessoa_id) %></span>
            <span class="detail-value"><%= g_pessoas_instrumento.g_pessoa %></span>
            <span class="detail-label"><%= GPessoasInstrumento.human_attribute_name(:g_instrumento_naipe_id) %></span>
            <span class="detail-value"><%= g_pessoas_instrumento.g_instrumento_naipe %></span>
          </div>
          <div class="record-card__footer">
            <%= render "shared/actions_menu", show_path: g_pessoas_instrumento_path(g_pessoas_instrumento), edit_path: edit_g_pessoas_instrumento_path(g_pessoas_instrumento), delete_path: g_pessoas_instrumento_path(g_pessoas_instrumento) %>
          </div>
        </article>
      <% end %>
    </div>

    <%= render "layouts/pagination", pagy: @pagy %>
  </section>
</div>
