<%= form_with(model: m_arranjador, local: true, class: "panel-card form-card") do |form| %>
  <%= render "shared/form_errors", record: m_arranjador %>

  <div class="form-grid">
    <div class="field-group">
      <%= form.label :descricao %>
      <%= form.text_field :descricao, class: "form-control", data: { controller: "uppercase" } %>
    </div>
  </div>

  <div class="form-actions">
    <%= form.submit class: "primary-button" %>
    <%= link_to "Cancelar", m_arranjador_path(@m_arranjador), class: "secondary-button" if m_arranjador.persisted? %>
    <%= link_to "Cancelar", m_arranjadores_path, class: "secondary-button" unless m_arranjador.persisted? %>
  </div>
<% end %>
