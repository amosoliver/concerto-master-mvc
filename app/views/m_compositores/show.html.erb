<div class="page-shell">
  <div class="page-header">
    <div>
      <p class="eyebrow">Detalhes</p>
      <h1><%= MCompositor.model_name.human %></h1>
    </div>
    <div class="page-header__actions">
      <%= authorized_link_to "Editar", edit_m_compositor_path(@m_compositor), class: "primary-button" %>
      <%= link_to "Voltar", m_compositores_path, class: "secondary-button" %>
    </div>
  </div>

  <section class="panel-card detail-card">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Informações</p>
        <h3><%= MCompositor.model_name.human %></h3>
      </div>
    </div>

    <div class="detail-grid">
      <div class="detail-tile">
        <span class="detail-label"><%= MCompositor.human_attribute_name(:descricao) %></span>
        <span class="detail-value"><%= @m_compositor.descricao %></span>
      </div>
    </div>

    <div class="detail-meta">
      <span>Criado em <%= l(@m_compositor.created_at) %></span>
      <span>Atualizado em <%= l(@m_compositor.updated_at) %></span>
    </div>
  </section>
</div>
