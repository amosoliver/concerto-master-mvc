<%= form_with(model: m_compositor, local: true, class: "panel-card form-card") do |form| %>
  <%= render "shared/form_errors", record: m_compositor %>

  <div class="form-grid">
    <div class="field-group">
      <%= form.label :descricao %>
      <%= form.text_field :descricao, class: "form-control", data: { controller: "uppercase" } %>
    </div>
  </div>

  <div class="form-actions">
    <%= form.submit class: "primary-button" %>
    <%= link_to "Cancelar", m_compositor_path(@m_compositor), class: "secondary-button" if m_compositor.persisted? %>
    <%= link_to "Cancelar", m_compositores_path, class: "secondary-button" unless m_compositor.persisted? %>
  </div>
<% end %>
