<div class="page-shell">
  <div class="page-header">
    <div>
      <p class="eyebrow">Detalhes</p>
      <h1><%= MTipoArranjo.model_name.human %></h1>
    </div>
    <div class="page-header__actions">
      <%= authorized_link_to "Editar", edit_m_tipo_arranjo_path(@m_tipo_arranjo), class: "primary-button" %>
      <%= link_to "Voltar", m_tipos_arranjos_path, class: "secondary-button" %>
    </div>
  </div>

  <section class="panel-card detail-card">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Informações</p>
        <h3><%= MTipoArranjo.model_name.human %></h3>
      </div>
    </div>

    <div class="detail-grid">
      <div class="detail-tile">
        <span class="detail-label"><%= MTipoArranjo.human_attribute_name(:descricao) %></span>
        <span class="detail-value"><%= @m_tipo_arranjo.descricao %></span>
      </div>
    </div>

    <div class="detail-meta">
      <span>Criado em <%= l(@m_tipo_arranjo.created_at) %></span>
      <span>Atualizado em <%= l(@m_tipo_arranjo.updated_at) %></span>
    </div>
  </section>
</div>
