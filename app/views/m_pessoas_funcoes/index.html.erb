<div class="page-shell" data-controller="filters" data-filters-active-value="<%= request.query_parameters.except("page").present? %>">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(MPessoaFuncao) %></h1>
    </div>
    <div class="page-header__actions">
      <button type="button" class="filter-toggle" data-filters-target="toggleButton" data-action="click->filters#toggle" aria-expanded="false">
        <span aria-hidden="true">🔎</span> Filtros
      </button>
      <%= link_to "Novo #{MPessoaFuncao.model_name.human}", new_m_pessoa_funcao_path, class: "primary-button" %>
    </div>
  </div>

  <div class="search-panel" data-filters-target="panel" hidden>
    <%= search_form_for @q, url: m_pessoas_funcoes_path, method: :get do |f| %>
      <div class="search-grid">
        <div class="search-field">
          <label><%= MPessoaFuncao.human_attribute_name(:g_pessoa_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :g_pessoa_id_eq, options_from_collection_for_select(GPessoa.all, :id, :to_s, @q&.send(:g_pessoa_id_eq)), { include_blank: true }, "aria-label": MPessoaFuncao.human_attribute_name(:g_pessoa_id), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= MPessoaFuncao.human_attribute_name(:u_funcao_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :u_funcao_id_eq, options_from_collection_for_select(UFuncao.all, :id, :to_s, @q&.send(:u_funcao_id_eq)), { include_blank: true }, "aria-label": MPessoaFuncao.human_attribute_name(:u_funcao_id), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= MPessoaFuncao.human_attribute_name(:principal) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.search_field :principal_cont, placeholder: MPessoaFuncao.human_attribute_name(:principal), "aria-label": MPessoaFuncao.human_attribute_name(:principal), class: "search-input__field" %>
          </div>
        </div>
      </div>
      <div class="search-panel__actions">
        <%= f.submit "Filtrar", class: "primary-button" %>
        <%= link_to "Limpar", m_pessoas_funcoes_path, class: "secondary-button" %>
      </div>
    <% end %>
  </div>

  <section class="panel-card" data-controller="view-toggle">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Listagem</p>
        <h3><%= pluralize(@m_pessoas_funcoes.size, "registro") %></h3>
      </div>
      <div class="view-switch">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable">☰ Tabela</button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards">▦ Cards</button>
      </div>
    </div>

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= MPessoaFuncao.human_attribute_name(:g_pessoa_id) %></th>
            <th scope="col"><%= MPessoaFuncao.human_attribute_name(:u_funcao_id) %></th>
            <th scope="col"><%= MPessoaFuncao.human_attribute_name(:principal) %></th>
            <th scope="col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @m_pessoas_funcoes.each do |m_pessoa_funcao| %>
            <tr>
              <td><%= m_pessoa_funcao.g_pessoa %></td>
              <td><%= m_pessoa_funcao.u_funcao %></td>
              <td><%= m_pessoa_funcao.principal %></td>
              <td>
                <%= render "shared/actions_menu", show_path: m_pessoa_funcao_path(m_pessoa_funcao), edit_path: edit_m_pessoa_funcao_path(m_pessoa_funcao), delete_path: m_pessoa_funcao_path(m_pessoa_funcao) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @m_pessoas_funcoes.each do |m_pessoa_funcao| %>
        <article class="record-card">
          <div class="record-card__body">
            <span class="detail-label"><%= MPessoaFuncao.human_attribute_name(:g_pessoa_id) %></span>
            <span class="detail-value"><%= m_pessoa_funcao.g_pessoa %></span>
            <span class="detail-label"><%= MPessoaFuncao.human_attribute_name(:u_funcao_id) %></span>
            <span class="detail-value"><%= m_pessoa_funcao.u_funcao %></span>
            <span class="detail-label"><%= MPessoaFuncao.human_attribute_name(:principal) %></span>
            <span class="detail-value"><%= m_pessoa_funcao.principal %></span>
          </div>
          <div class="record-card__footer">
            <%= render "shared/actions_menu", show_path: m_pessoa_funcao_path(m_pessoa_funcao), edit_path: edit_m_pessoa_funcao_path(m_pessoa_funcao), delete_path: m_pessoa_funcao_path(m_pessoa_funcao) %>
          </div>
        </article>
      <% end %>
    </div>

    <%= render "layouts/pagination", pagy: @pagy %>
  </section>
</div>
