<div class="page-shell" data-controller="filters" data-filters-active-value="<%= request.query_parameters.except("page").present? %>">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(GMunicipio) %></h1>
    </div>
    <div class="page-header__actions">
      <button type="button" class="filter-toggle" data-filters-target="toggleButton" data-action="click->filters#toggle" aria-expanded="false">
        <span aria-hidden="true">🔎</span> Filtros
      </button>
      <%= authorized_link_to "Novo #{GMunicipio.model_name.human}", new_g_municipio_path, class: "primary-button" %>
    </div>
  </div>

  <div class="search-panel" data-filters-target="panel" hidden>
    <%= search_form_for @q, url: g_municipios_path, method: :get do |f| %>
      <div class="search-grid">
        <div class="search-field">
          <label><%= GMunicipio.human_attribute_name(:descricao) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.search_field :descricao_cont, placeholder: GMunicipio.human_attribute_name(:descricao), "aria-label": GMunicipio.human_attribute_name(:descricao), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= GMunicipio.human_attribute_name(:codigo_ibge) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.search_field :codigo_ibge_cont, placeholder: GMunicipio.human_attribute_name(:codigo_ibge), "aria-label": GMunicipio.human_attribute_name(:codigo_ibge), class: "search-input__field" %>
          </div>
        </div>
        <div class="search-field">
          <label><%= GMunicipio.human_attribute_name(:g_estado_id) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.select :g_estado_id_eq, options_from_collection_for_select(GEstado.all, :id, :to_s, @q&.send(:g_estado_id_eq)), { include_blank: true }, "aria-label": GMunicipio.human_attribute_name(:g_estado_id), class: "search-input__field" %>
          </div>
        </div>
      </div>
      <div class="search-panel__actions">
        <%= f.submit "Filtrar", class: "primary-button" %>
        <%= link_to "Limpar", g_municipios_path, class: "secondary-button" %>
      </div>
    <% end %>
  </div>

  <section class="panel-card panel-card--fill" data-controller="view-toggle">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Listagem</p>
        <h3><%= pluralize(@g_municipios.size, "registro") %></h3>
      </div>
      <div class="view-switch">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable">☰ Tabela</button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards">▦ Cards</button>
      </div>
    </div>

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= GMunicipio.human_attribute_name(:descricao) %></th>
            <th scope="col"><%= GMunicipio.human_attribute_name(:codigo_ibge) %></th>
            <th scope="col"><%= GMunicipio.human_attribute_name(:g_estado_id) %></th>
            <th scope="col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @g_municipios.each do |g_municipio| %>
            <tr>
              <td><%= g_municipio.descricao %></td>
              <td><%= g_municipio.codigo_ibge %></td>
              <td><%= g_municipio.g_estado %></td>
              <td>
                <%= render "shared/actions_menu", show_path: g_municipio_path(g_municipio), edit_path: edit_g_municipio_path(g_municipio), delete_path: g_municipio_path(g_municipio) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @g_municipios.each do |g_municipio| %>
        <article class="record-card">
          <div class="record-card__body">
            <span class="detail-label"><%= GMunicipio.human_attribute_name(:descricao) %></span>
            <span class="detail-value"><%= g_municipio.descricao %></span>
            <span class="detail-label"><%= GMunicipio.human_attribute_name(:codigo_ibge) %></span>
            <span class="detail-value"><%= g_municipio.codigo_ibge %></span>
            <span class="detail-label"><%= GMunicipio.human_attribute_name(:g_estado_id) %></span>
            <span class="detail-value"><%= g_municipio.g_estado %></span>
          </div>
          <div class="record-card__footer">
            <%= render "shared/actions_menu", show_path: g_municipio_path(g_municipio), edit_path: edit_g_municipio_path(g_municipio), delete_path: g_municipio_path(g_municipio) %>
          </div>
        </article>
      <% end %>
    </div>

    <%= render "layouts/pagination", pagy: @pagy %>
  </section>
</div>
