<div class="page-shell">
  <div class="page-header">
    <div>
      <p class="eyebrow">Editar</p>
      <h1>Editar <%= GPessoa.model_name.human %> - <%= current_step.titleize %></h1>
    </div>
  </div>

  <%= render "form", g_pessoa: @g_pessoa %>
</div>
