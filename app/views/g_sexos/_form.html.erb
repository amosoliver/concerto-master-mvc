<%= form_with(model: g_sexo, local: true, class: "panel-card form-card") do |form| %>
  <% if g_sexo.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(g_sexo.errors.count, "erro") %> impediram o salvamento:</h2>
      <ul>
        <% g_sexo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-grid">
    <div class="field-group">
      <%= form.label :descricao %>
      <%= form.text_field :descricao, class: "form-control" %>
    </div>
  </div>

  <div class="form-actions">
    <%= form.submit class: "primary-button" %>
    <%= link_to "Cancelar", g_sexo_path(@g_sexo), class: "secondary-button" if g_sexo.persisted? %>
    <%= link_to "Cancelar", g_sexos_path, class: "secondary-button" unless g_sexo.persisted? %>
  </div>
<% end %>
