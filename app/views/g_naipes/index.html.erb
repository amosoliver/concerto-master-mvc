<div class="page-shell" data-controller="filters" data-filters-active-value="<%= request.query_parameters.except("page").present? %>">
  <div class="page-header">
    <div>
      <p class="eyebrow">Gerenciamento</p>
      <h1><%= model_plural_human_name(GNaipe) %></h1>
    </div>
    <div class="page-header__actions">
      <button type="button" class="filter-toggle" data-filters-target="toggleButton" data-action="click->filters#toggle" aria-expanded="false">
        <span aria-hidden="true">🔎</span> Filtros
      </button>
      <%= authorized_link_to "Novo #{GNaipe.model_name.human}", new_g_naipe_path, class: "primary-button" %>
    </div>
  </div>

  <div class="search-panel" data-filters-target="panel" hidden>
    <%= search_form_for @q, url: g_naipes_path, method: :get do |f| %>
      <div class="search-grid">
        <div class="search-field">
          <label><%= GNaipe.human_attribute_name(:descricao) %></label>
          <div class="search-input">
            <span aria-hidden="true">🔎</span>
            <%= f.search_field :descricao_cont, placeholder: GNaipe.human_attribute_name(:descricao), "aria-label": GNaipe.human_attribute_name(:descricao), class: "search-input__field" %>
          </div>
        </div>
      </div>
      <div class="search-panel__actions">
        <%= f.submit "Filtrar", class: "primary-button" %>
        <%= link_to "Limpar", g_naipes_path, class: "secondary-button" %>
      </div>
    <% end %>
  </div>

  <section class="panel-card" data-controller="view-toggle">
    <div class="panel-card__header">
      <div>
        <p class="eyebrow">Listagem</p>
        <h3><%= pluralize(@g_naipes.size, "registro") %></h3>
      </div>
      <div class="view-switch">
        <button type="button" class="view-switch__btn is-active" data-view-toggle-target="tableButton" data-action="click->view-toggle#showTable">☰ Tabela</button>
        <button type="button" class="view-switch__btn" data-view-toggle-target="cardsButton" data-action="click->view-toggle#showCards">▦ Cards</button>
      </div>
    </div>

    <div class="table-wrapper" data-view-toggle-target="table">
      <table class="data-table">
        <thead>
          <tr>
            <th scope="col"><%= GNaipe.human_attribute_name(:descricao) %></th>
            <th scope="col">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @g_naipes.each do |g_naipe| %>
            <tr>
              <td><%= g_naipe.descricao %></td>
              <td>
                <%= render "shared/actions_menu", show_path: g_naipe_path(g_naipe), edit_path: edit_g_naipe_path(g_naipe), delete_path: g_naipe_path(g_naipe) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-grid" data-view-toggle-target="cards" hidden>
      <% @g_naipes.each do |g_naipe| %>
        <article class="record-card">
          <div class="record-card__body">
            <span class="detail-label"><%= GNaipe.human_attribute_name(:descricao) %></span>
            <span class="detail-value"><%= g_naipe.descricao %></span>
          </div>
          <div class="record-card__footer">
            <%= render "shared/actions_menu", show_path: g_naipe_path(g_naipe), edit_path: edit_g_naipe_path(g_naipe), delete_path: g_naipe_path(g_naipe) %>
          </div>
        </article>
      <% end %>
    </div>

    <%= render "layouts/pagination", pagy: @pagy %>
  </section>
</div>
